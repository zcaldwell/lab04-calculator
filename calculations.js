export function add(x, y) {
    let sum = x + y;
    return sum;

}

export function sub(x, y) {
    let sum = x - y;
    return sum;
}

export function times(x, y) {
    let sum = x * y;
    return sum;
}